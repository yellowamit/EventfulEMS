{
  "name": "eventfulems",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix api && npm install --include=dev --prefix client",
    "build:client": "npm run build --prefix client",
    "build": "npm run install:all && npm run build:client",
    "start": "npm start --prefix api",
    "dev": "npm run dev --prefix api & npm run dev --prefix client",
    "render-build": "npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
